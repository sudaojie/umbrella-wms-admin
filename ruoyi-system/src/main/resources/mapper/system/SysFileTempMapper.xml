<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysFileTempMapper">

    <resultMap type="com.ruoyi.system.domain.SysFileTemp" id="SysFileTempResult">
        <result property="id" column="id"/>
        <result property="buinessId" column="buiness_id"/>
        <result property="type" column="type"/>
        <result property="displayName" column="display_name"/>
        <result property="name" column="name"/>
        <result property="path" column="path"/>
        <result property="size" column="size"/>
        <result property="orderNo" column="order_no"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectSysFileTempVo">
        select
            id,
            buiness_id,
            `type`,
            display_name,
            `name`,
            path,
            `size`,
            order_no,
            del_flag,
            create_by,
            create_time,
            update_by,
            update_time,
            remark
        from
            sys_file_temp
    </sql>

    <select id="selectSysFileTempList" parameterType="com.ruoyi.system.domain.SysFileTemp"
            resultMap="SysFileTempResult">
        <include refid="selectSysFileTempVo"/>
        <where>
            <if test="type != null  and type != ''">and `type` = #{type}</if>
            <if test="displayName != null  and displayName != ''">and display_name like concat('%', #{displayName},'%')</if>
            <if test="name != null  and name != ''">and `name` like concat('%', #{name}, '%')</if>
        </where>
    </select>

</mapper>