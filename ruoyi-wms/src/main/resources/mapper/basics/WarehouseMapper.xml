<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.wms.basics.mapper.WarehouseMapper">

    <resultMap type="com.ruoyi.wms.basics.domain.Warehouse" id="WarehouseResult">
        <result property="id" column="id"/>
        <result property="warehouseCode" column="warehouse_code"/>
        <result property="warehouseName" column="warehouse_name"/>
        <result property="totalCapacity" column="total_capacity"/>
        <result property="availableCapacity" column="available_capacity"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <select id="select" resultType="com.ruoyi.wms.basics.domain.Warehouse">
        select
        id,
        warehouse_code,
        warehouse_name,
        total_capacity,
        available_capacity,
        del_flag,
        create_by,
        create_time,
        update_by,
        update_time,
        remark
        from
        wms_warehouse_info
        <where>
            ${ew.sqlSegment}
        </where>
        order by create_time desc
    </select>
    <select id="checkCode" resultType="java.lang.Integer">
        select count(*) from wms_warehouse_info
        where warehouse_code=#{object.warehouseCode}  and del_flag = '0'
        <if test="object.id!=null and object.id!=''">
            and id != #{object.id}
        </if>
    </select>
    <select id="checkName" resultType="java.lang.Integer">
        select count(*) from wms_warehouse_info
        where warehouse_name=#{object.warehouseName}  and del_flag = '0'
        <if test="object.id!=null and object.id!=''">
            and id != #{object.id}
        </if>
    </select>
    <select id="getWarehouseData" resultType="java.util.Map">
        select warehouse_code value,
               CONCAT(warehouse_code,'-',warehouse_name)   label
        from wms_warehouse_info
        where del_flag = '0' order by warehouse_code asc
    </select>
    <select id="selectDataByCode" resultType="com.ruoyi.wms.basics.domain.Warehouse">
        select * from wms_warehouse_info where del_flag = '0' and warehouse_code = #{warehouseCode}
    </select>


</mapper>
