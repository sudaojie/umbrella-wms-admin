<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.wms.stock.mapper.WmsWarningConfigMapper">

    <resultMap type="com.ruoyi.wms.stock.domain.WmsWarningConfig" id="WmsWarningConfigResult">
        <result property="id" column="ID"/>
        <result property="configName" column="config_name"/>
        <result property="configKey" column="config_key"/>
        <result property="configValue" column="config_value"/>
        <result property="configType" column="config_type"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="delFlag" column="del_flag"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <select id="select" resultType="com.ruoyi.wms.stock.domain.WmsWarningConfig">
        select
            ID,
            config_name,
            config_key,
            config_value,
            config_type,
            create_by,
            create_time,
            update_by,
            del_flag,
            update_time,
            remark
        from
            wms_warning_config
        <where>
            ${ew.sqlSegment}
        </where>
    </select>

    <select id="selectConfigByValue" resultType="java.lang.Integer">
        select
            count(1)
        from
        wms_warning_config
        <where>
            ${ew.sqlSegment}
        </where>
    </select>

</mapper>
